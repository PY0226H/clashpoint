# Frontend Workspace

This workspace is the React + TypeScript baseline for EchoIsle Web and Desktop (Tauri).

## Quick start

```bash
cd frontend
pnpm install
pnpm dev:web
```

Desktop shell:

```bash
cd frontend
pnpm dev:desktop:tauri
```
