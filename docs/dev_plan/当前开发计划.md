# 当前开发计划

关联 slot：`default`  
更新时间：2026-04-15  
当前主线：`AI_judge_service 下一阶段（P10：Fairness Hardened Phase2 + Adaptive Agent Entrypoints）`  
当前状态：执行中（持续推进）
归档来源：`docs/dev_plan/archive/20260415T011924Z-ai-judge-plan-reset-archive.md`

---

## 1. 计划定位

1. 本计划承接 P8/P9 已完成能力，进入“公平治理制度化 + Agent 能力入口化”的下一阶段。
2. 本轮继续执行硬切原则：不保留兼容层、灰度双轨、旧新并行主链。
3. 真实环境相关项保持“可执行入口 + 阻塞显式标记”，不将本机结果写成 real pass。
4. 计划依据：
   - `docs/dev_plan/AI_Judge_Service-架构与技术栈决策方案-2026-04-13.md`
   - `docs/dev_plan/AI_Judge_Service-企业级Agent服务设计方案-2026-04-13.md`
   - 当前代码主链与最新收口证据（runtime ops pack / fairness ingest / real-env window local reference）

---

## 2. 当前完成度快照（作为 P10 起点）

1. 平台化主链已完成：workflow/orchestrator、gateway、judge core、trace/replay/audit、cases 硬切。
2. 信任与公平 PhaseB 主链已完成：challenge 状态机、registry 发布态（policy）、claim ledger v3、panel 独立判定 v1、fairness drift governance v1。
3. 收口自动化已完成：runtime ops pack、fairness benchmark freeze、fairness ingest 自动上报、real-env window ingest 透传。
4. 当前剩余关键缺口：
   - real-env 复核仍处于 `local_reference_ready`，未达 `pass`。
   - Prompt/Tool registry 仍未与 policy registry 同级产品化。
   - fairness 与 registry 激活之间尚未形成“发布门禁”闭环。
   - 面向未来 NPC Coach / Room QA 的 Agent 入口尚停留在设计层。

---

## 3. 下一阶段总目标（P10）

在不拆服务、不引入长期兼容层的前提下，把 AI_judge_service 从“主链可用”推进到“可持续治理 + 可扩展 Agent 平台入口可用”。

P10 完成定义（全量）：

1. real-env 收口能力可一键复跑并具备明确 pass 判定标准（环境就绪后可落 real pass）。
2. registry 从 policy 单点扩展为 `prompt + tool + policy` 三类发布态主链。
3. fairness benchmark / drift 与 registry 激活形成强绑定门禁。
4. panel 从结构独立升级到“运行配置独立 + 可审计回放”。
5. 新增内部 Agent 入口壳：NPC Coach 与 Room QA（仅内部接口，不对用户开放）。

---

## 4. P10 模块执行矩阵

| 模块 | 优先级 | 状态 | 本轮目标 | DoD（完成定义） | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| `ai-judge-p10-real-env-hard-pass-closure` | P0（环境阻塞） | 进行中（local_reference_ready） | 将 real-env window 收口口径升级为“可直接验收 real pass” | `ai_judge_real_env_window_closure.sh` 在 real 环境可达 `pass`；`runtime_ops_pack` 四子模块均 `pass`；`FAIRNESS_INGEST_STATUS=sent`；收口证据写入 `docs/loadtest/evidence` | `bash scripts/harness/ai_judge_real_env_window_closure.sh --root /Users/panyihang/Documents/EchoIsle`（环境就绪后）；本机参考保留 `--allow-local-reference` |
| `ai-judge-p10-registry-triple-productization` | P0 | 进行中（phase1 已完成） | 补齐 Prompt/Tool Registry 的发布/激活/审计主链 | 新增 prompt/tool release + audit 存储与路由；phase/final/panel runtime 统一从 DB registry 解析；移除 env-only 主路径 | `cd ai_judge_service && ../scripts/py -m pytest -q`; 新增 app_factory / repository / contract 用例 |
| `ai-judge-p10-fairness-release-gate-v2` | P1 | 进行中（phase1 已完成） | 将 fairness drift 结果绑定到 registry 激活门禁 | registry activate 前执行 benchmark gate；阈值越界默认阻断（可审计 override）；告警/outbox 落库可追踪 | `cd ai_judge_service && ../scripts/py -m pytest -q`; `tests/test_app_factory.py` 增加 activate gate 分支 |
| `ai-judge-p10-panel-runtime-profile-v2` | P1 | 进行中（phase1 已完成） | 面板判定从“结构独立”升级为“运行配置独立” | judgeA/B/C 支持独立 profile/model strategy；report/trace/replay 持久化 profile 元信息；分歧解释字段对齐 | `cd ai_judge_service && ../scripts/py -m pytest -q`; `tests/test_phase_pipeline.py` + `tests/test_app_factory.py` |
| `ai-judge-p10-case-fairness-read-model` | P1 | 进行中（phase1+phase2.1 已完成） | 形成面向 Ops 的 case 级公平读模型 | 新增 case fairness 读接口（含 gate 结论、panel 分歧、drift 摘要、review/challenge 关联）并可按条件检索 | `cd ai_judge_service && ../scripts/py -m pytest -q`; 路由与仓储查询测试 |
| `ai-judge-p10-agent-entrypoints-shell` | P2 | 待开始 | 为 NPC Coach / Room QA 建立内部 Agent 入口壳 | 新增内部接口契约与最小 runtime（读取房间上下文、阶段摘要、规则版本）；明确“建议型输出，不参与官方裁决链” | `cd ai_judge_service && ../scripts/py -m pytest -q`; 新增 `tests/test_agent_runtime.py`/路由测试 |

---

## 5. 执行顺序与依赖

1. 先推进 `ai-judge-p10-registry-triple-productization`，因为 fairness gate v2 与 panel profile 都依赖统一 registry。
2. 再推进 `ai-judge-p10-fairness-release-gate-v2`，把“可发布”与“公平可接受”绑定。
3. 然后推进 `ai-judge-p10-panel-runtime-profile-v2`，补齐多 panel 独立运行证据。
4. 接着推进 `ai-judge-p10-case-fairness-read-model`，确保 Ops 可读可追溯。
5. 再落 `ai-judge-p10-agent-entrypoints-shell`，建立 NPC/Room QA 入口但不侵入裁决主链。
6. 最后在环境可用时收口 `ai-judge-p10-real-env-hard-pass-closure`。

---

## 6. 本阶段明确不做

1. 不引入长期兼容层、双轨 payload、alias 并存。
2. 不在本阶段引入 Temporal/Kafka 重构，保持当前 orchestrator 主链。
3. 不将 NPC Coach / Room QA 直接开放给用户侧主流程，仅做内部接口壳与策略边界。
4. 不把区块链/ZK/ZKML 作为生产主链依赖，仅保留协议化扩展接口约束。

---

## 7. 测试与验收基线

1. 回归基线：`cd ai_judge_service && ../scripts/py -m pytest -q`
2. 质量基线：`cd ai_judge_service && ../scripts/py -m ruff check app tests`
3. runtime 收口基线：`bash scripts/harness/ai_judge_runtime_ops_pack.sh --root /Users/panyihang/Documents/EchoIsle --allow-local-reference`
4. real-env 收口基线：`bash scripts/harness/ai_judge_real_env_window_closure.sh --root /Users/panyihang/Documents/EchoIsle`
5. 计划一致性基线：`当前开发计划.md` 与后续 completed/todo 的模块状态一致。

---

## 8. 风险与对策

1. 风险：registry 三分化后，调用链更复杂，易出现解析不一致。  
   对策：所有 runtime 统一走单一 registry resolver；新增端到端契约测试覆盖 phase/final/replay。
2. 风险：fairness gate 绑定发布可能抬高发布门槛，影响研发效率。  
   对策：保留可审计 override 机制，但默认阻断；所有 override 必须落审计原因与责任人。
3. 风险：panel profile 独立会抬升时延与成本。  
   对策：先做可配置 profile，不默认开启高成本策略；通过 runtime SLA freeze 持续观测。
4. 风险：real-env 长期不可用，导致 P10 末端持续阻塞。  
   对策：保持 local_reference 收口证据持续刷新，等待环境窗口时一键复跑并收口为 pass。

---

## 9. 本轮启动检查清单

1. 先完成 `registry-triple-productization` 的领域模型与路由骨架设计评审。
2. 在 `app_factory` 落地 registry 三分化读取主链，并补第一批契约测试。
3. 设计 `fairness-release-gate-v2` 的阻断/override 状态机与错误语义。
4. 将上述三项落盘到本计划的“模块完成同步历史”并逐模块推进。

---

## 10. 模块完成同步历史（P10）

- 2026-04-15：初始化 P10 计划；归档上一阶段计划到 `docs/dev_plan/archive/20260415T011924Z-ai-judge-plan-reset-archive.md`。
- 2026-04-15：启动 `ai-judge-p10-registry-triple-productization`（phase1）：新增 registry release 查询读接口 `GET /internal/judge/registries/{registry_type}/releases` 与 `GET /internal/judge/registries/{registry_type}/releases/{version}`，补齐发布态查询闭环；并通过 `tests/test_app_factory.py`、`tests/test_registry_runtime.py`、`tests/test_policy_registry_runtime.py` 定向回归。
- 2026-04-15：启动 `ai-judge-p10-fairness-release-gate-v2`（phase1）：对 `policy` 的 `publish(activate=true)` / `activate` 接口接入 fairness gate（默认阻断，支持 `override_fairness_gate` + 必填 reason 的可审计覆盖）；新增门禁阻断告警 `registry_fairness_gate_blocked` 与覆盖告警 `registry_fairness_gate_override`（同步 trace/facts 审计链）；并补充 `tests/test_app_factory.py` 覆盖阻断与 override 分支。
- 2026-04-15：启动 `ai-judge-p10-panel-runtime-profile-v2`（phase1）：final 主链与 replay 主链支持从 policy metadata 解析 `judgeA/B/C` 独立 panel runtime profile（profileId/modelStrategy/scoreSource/decisionMargin/promptVersion/toolsetVersion/policyVersion），并将元信息落入 `verdictLedger.panelDecisions.runtimeProfiles`、`judgeTrace.panelRuntimeProfiles` 与 `judgeTrace.panelArbiter.runtimeProfiles`；同时将分歧告警细节补充 `panelRuntimeProfiles`，并完成 `tests/test_judge_mainline.py`、`tests/test_app_factory.py`、`tests/test_phase_final_contract_models.py` 回归。
- 2026-04-15：启动 `ai-judge-p10-case-fairness-read-model`（phase1）：新增 case 级公平读接口 `GET /internal/judge/fairness/cases/{case_id}` 与 `GET /internal/judge/fairness/cases`，输出 gate 结论、panel 分歧、runtime profile 元信息、drift 摘要与 challenge 关联快照；并补充 `tests/test_app_factory.py` 路由曝光与读模型查询回归。
- 2026-04-15：推进 `ai-judge-p10-case-fairness-read-model`（phase2.1）：`GET /internal/judge/fairness/cases` 新增 `gate_conclusion` / `challenge_state` 过滤与 `offset+limit` 分页返回（含 `count/returned`）；修复 challenge 事件快照字段解析（兼容 `challengeState`）；并补充 `tests/test_app_factory.py` 覆盖筛选、分页与非法参数分支。
