# 当前开发计划

关联 slot：`default`  
更新时间：2026-04-23  
当前主线：`AI_judge_service P33（read/panel/review 热点下沉 + 企业方案一致性强化 + real-env pass 准备）`  
当前状态：执行中（P32 阶段已归档；P33 已完成一百一十八批，当前推进到 `app_factory hotspot split` 第 107 子批：`registry fairness/dependency helper hoist cleanup` + `shared-room-context builder hoist cleanup` + `workflow/audit runtime wrapper hoist cleanup` + `fairness benchmark/shadow runtime wrapper hoist cleanup` + `dispatch/claim/replay runtime wrapper hoist cleanup` + `workflow-mark/sync-alert/alert-transition runtime helper hoist cleanup` + `dispatch-receipt/claim-ledger writer wrapper hoist cleanup` + `trust-context/phasea-bundle runtime wrapper hoist cleanup` + `replay-dependency-pack builder hoist cleanup` + `trust/alert runtime partial bootstrap centralization cleanup` + `registry-governance dependency-pack factory reuse cleanup` + `workflow-schema/registry-readiness runtime wrapper hoist cleanup` + `prompt/tool registry payload helper reuse cleanup` + `trust validated-item route helper reuse cleanup` + `trust anchor/public-verify route helper reuse cleanup` + `registry-release gate dependency-pack reuse cleanup` + `trust-challenge common dependency-pack reuse cleanup` + `review-risk/trust-priority runtime wrapper hoist cleanup` + `courtroom/review/trust-challenge lambda-wrapper reuse cleanup` + `courtroom read-model/drilldown runtime wrapper hoist cleanup` + `fairness datetime/item/aggregation runtime wrapper hoist cleanup` + `case-read/fairness lambda-wrapper reuse cleanup` + `judge-command shared dependency injection reuse cleanup` + `replay-dependency-pack shared dependency injection reuse cleanup` + `trust-read/registry-governance lambda-wrapper cleanup` + `lambda-zero baseline cleanup` + `route-guard http-bridge helper reuse cleanup` + `policy registry payload helper reuse cleanup` + `dispatch-receipt route helper reuse cleanup` + `case-read contract-validator helper reuse cleanup` + `ops/panel contract-awaitable helper reuse cleanup` + `policy/prompt/tool registry ready-payload helper reuse cleanup` + `review/alert await-error bridge helper reuse cleanup` + `review decision contract-bridge helper reuse cleanup` + `alert outbox/rag lookup-bridge helper reuse cleanup` + `registry version-not-found lookup bridge helper reuse cleanup` + `registry audits/releases payload helper reuse cleanup` + `review-list payload helper reuse cleanup` + `ops-pack/review-detail/alert-ops payload helper reuse cleanup` + `case-alert/outbox payload helper reuse cleanup` + `panel-runtime profiles/readiness payload helper reuse cleanup` + `replay-report/fairness-calibration payload helper reuse cleanup` + `trust-challenge payload helper reuse cleanup`）

---

## 1. 计划定位

1. 本计划承接归档：`/Users/panyihang/Documents/EchoIsle/docs/dev_plan/archive/20260422T053246Z-ai-judge-current-plan-archive.md`。
2. P32 已完成主链进展：
   - `judge_command_routes` 已完成 phase/final dispatch 关键编排下沉（preflight、blindization reject、callback result/delivery、final contract blocked、report materialization）。
   - `case_read_routes` 已完成 `GET /internal/judge/cases/{case_id}` 编排下沉。
3. P32 延续到 P33 的核心未收口项：
   - `app_factory` 仍是结构热点（当前约 `5626` 行）。
   - `real-env pass window` 仍受真实环境阻塞，仅能维持 `on-env`。
4. P33 目标：在不改业务语义的前提下，完成读取链路与运维读面下沉，落实企业方案与架构方案一致性约束。

---

## 2. 当前代码状态快照（P33 起点）

截至 2026-04-22，`ai_judge_service` 现状：

1. 主链能力已具备：`trace/replay/failed callback/fairness/review/challenge/trust` 可在本地回归。
2. 已完成结构化下沉：
   - 命令路由：`app/applications/judge_command_routes.py`（约 `1411` 行）。
   - case 读路由：`app/applications/case_read_routes.py`（约 `1415` 行）。
3. 仍待收口热点：
   - `app_factory` 仍有局部纯函数热点待继续下沉（当前已完成至第 `107` 子批 direct-wiring 收口）。
   - `real-env pass window` 仍处于 `on-env` 阻塞态。
4. 环境约束不变：当前只有本地环境，真实环境验证保持 `on-env` 标签，不可记为 `pass`。

---

## 3. 企业方案与架构方案对齐锚点（P33 执行硬约束）

1. 角色边界不塌缩：保持 8 Agent 职责分工，不把 Judge/NPC/Room QA 混成单一裁决链。
2. 六对象主链不漂移：`case_dossier/claim_graph/evidence_ledger/verdict_ledger/fairness_report/opinion_pack` 仍是唯一主语义。
3. 公平门禁不后移：`Fairness Sentinel` 必须保持在终判前，override 行为可审计。
4. 契约单轨：未发布能力不保留长期 alias/双写/兼容层，保持硬切主链。
5. NPC/Room QA 严格 advisory-only：不得写入官方 `verdict/fairness/trust` 账本字段。

---

## 4. P33 模块执行矩阵

### 已完成/未完成矩阵

| 模块 | 优先级 | 状态 | 本轮目标 | DoD（完成定义） | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| `ai-judge-next-iteration-planning` | P0 | 已完成（2026-04-22） | P32 收口后生成 P33 完整计划 | 当前计划切换到 P33，承接归档并列出剩余热点 | `bash scripts/harness/ai_judge_plan_consistency_gate.sh --root /Users/panyihang/Documents/EchoIsle` |
| `ai-judge-p33-app-factory-hotspot-split-v4` | P0 | 进行中（已推进第 107 子批，2026-04-23） | 继续缩减 `app_factory` 热点 | 已完成：`case_evidence/evidence_claim` + `courtroom_read_model_view/light_summary/drilldown_bundle/action_hints` + `claim_ledger/courtroom sort/evidence_claim sort&filter` + `trust_challenge filter/priority/sort/action/unified_priority` + `review_case filter/risk/sort` + `case_fairness filter/sort/aggregations` + `panel_runtime profile/readiness filter/sort/item/aggregations/summary` + `registry_alert ops filter/outbox index` + `registry_dependency overview/trend filter` + `registry_prompt_tool risk usage/items/action_hints` + `registry_alert trend/item serializer/audit link index` + `case_fairness item builder/challenge snapshot` + `fairness calibration on-env/advisor helper` + `blindization sensitive-hit scanner` + `judge-command error-contract/failed-callback payload helper` + `judge-command dispatch-meta/receipt-dims helper` + `judge-command idempotency/validator/callback-selector helper` + `judge-command payload-extract helper` + `judge-command callback-retry/receipt-persist helper` + `judge-command safe-float/winner helper` + `judge-command final-report-payload helper` + `judge-command registry policy/prompt/tool profile resolver helper` + `judge-command policy trace snapshot helper` + `judge-command panel runtime profile resolver helper` + `judge-command trace/workflow direct wiring helper cleanup` + `judge-command attach-report-attestation direct wiring helper cleanup` + `registry prompt/tool serializer + ensure-loaded direct wiring helper cleanup` + `judge-command final-report-payload direct wiring helper cleanup` + `alert/outbox/dispatch serializer direct wiring helper cleanup` + `verdict-contract direct wiring helper cleanup` + `case-evidence-view direct wiring helper cleanup` + `claim-ledger serializer direct wiring helper cleanup` + `replay safe-float/winner direct wiring helper cleanup` + `judge-command error/failed-callback direct wiring helper cleanup` + `judge-command blindization meta-parser direct wiring helper cleanup` + `judge-command idempotency resolver direct wiring helper cleanup` + `fairness upsert parser direct wiring helper cleanup` + `courtroom/evidence sort-normalizer direct wiring helper cleanup` + `review-case sort-normalizer direct wiring helper cleanup` + `trust-challenge sort-normalizer direct wiring helper cleanup` + `case-fairness sort-normalizer direct wiring helper cleanup` + `panel-runtime profile-normalizer direct-wiring helper cleanup` + `registry normalize direct-wiring helper cleanup` + `cross-route sort-key direct-wiring helper cleanup` + `panel-runtime sort-key/aggregation direct-wiring helper cleanup` + `courtroom/evidence profile-hint direct-wiring helper cleanup` + `fairness dashboard/calibration direct-wiring helper cleanup` + `registry dependency/prompt-tool direct-wiring helper cleanup` + `registry alert/audit + panel-runtime item direct-wiring helper cleanup` + `read/trust/fairness/panel contract-validator direct-wiring helper cleanup` + `replay report-deps inline-resolver direct-wiring helper cleanup` + `replay dependency-pack route-inline cleanup` + `callback-selector/receipt-persist/final-report-contract direct-wiring helper cleanup` + `resolver-profile direct-wiring helper cleanup` + `policy-trace-snapshot direct-wiring helper cleanup` + `case-dossier payload-helper consolidation cleanup` + `policy-serializer callback-signature compat fix` + `assistant-response builder downshift` + `policy-domain-family serializer downshift` + `workflow-job builder downshift` + `app-factory error/guard helper hoist cleanup` + `registry-governance-pack builder hoist cleanup` + `case-dossier payload inline-parser helper hoist cleanup` + `request-json parser helper hoist cleanup` + `registry-fairness/dependency helper hoist cleanup` + `shared-room-context builder hoist cleanup` + `workflow/audit runtime wrapper hoist cleanup` + `fairness benchmark/shadow runtime wrapper hoist cleanup` + `dispatch/claim/replay runtime wrapper hoist cleanup` + `workflow-mark/sync-alert/alert-transition runtime helper hoist cleanup` + `dispatch-receipt/claim-ledger writer wrapper hoist cleanup` + `trust-context/phasea-bundle runtime wrapper hoist cleanup` + `replay-dependency-pack builder hoist cleanup` + `trust/alert runtime partial bootstrap centralization cleanup` + `registry-governance dependency-pack factory reuse cleanup` + `workflow-schema/registry-readiness runtime wrapper hoist cleanup` + `prompt/tool registry payload helper reuse cleanup` + `trust validated-item route helper reuse cleanup` + `trust anchor/public-verify route helper reuse cleanup` + `registry-release gate dependency-pack reuse cleanup` + `trust-challenge common dependency-pack reuse cleanup` + `review-risk/trust-priority runtime wrapper hoist cleanup` + `courtroom/review/trust-challenge lambda-wrapper reuse cleanup` + `courtroom read-model/drilldown runtime wrapper hoist cleanup` + `fairness datetime/item/aggregation runtime wrapper hoist cleanup` + `case-read/fairness lambda-wrapper reuse cleanup` + `judge-command shared dependency injection reuse cleanup` + `replay-dependency-pack shared dependency injection reuse cleanup` + `trust-read/registry-governance lambda-wrapper cleanup` + `lambda-zero baseline cleanup` + `route-guard http-bridge helper reuse cleanup` + `policy registry payload helper reuse cleanup` + `dispatch-receipt route helper reuse cleanup` + `case-read contract-validator helper reuse cleanup` + `ops/panel contract-awaitable helper reuse cleanup` + `policy/prompt/tool registry ready-payload helper reuse cleanup` + `review/alert await-error bridge helper reuse cleanup` + `review decision contract-bridge helper reuse cleanup` + `alert outbox/rag lookup-bridge helper reuse cleanup` + `registry version-not-found lookup bridge helper reuse cleanup` + `registry audits/releases payload helper reuse cleanup` + `review-list payload helper reuse cleanup` + `ops-pack/review-detail/alert-ops payload helper reuse cleanup` + `case-alert/outbox payload helper reuse cleanup` + `panel-runtime profiles/readiness payload helper reuse cleanup` + `replay-report/fairness-calibration payload helper reuse cleanup` + `trust-challenge payload helper reuse cleanup` 构建与归一化函数下沉到 applications 层，`app_factory` 改为薄包装调用；待继续下沉剩余非核心 helper 热点 | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_case_read_routes.py tests/test_app_factory.py -k "case_detail_route_should_aggregate_case_snapshot or courtroom_read_model or courtroom_drilldown_bundle or evidence_claim_ops_queue or claim_ledger"` + `cd ai_judge_service && ../scripts/py -m ruff check app/applications tests` |
| `ai-judge-p33-judge-read-route-orchestrator-v2` | P0 | 已完成（2026-04-22） | 下沉剩余 `judge read/courtroom` 链路 | 五批全部完成：`/internal/judge/cases/{case_id}/claim-ledger`、`/internal/judge/cases/{case_id}/courtroom-read-model`、`/internal/judge/courtroom/cases`、`/internal/judge/courtroom/drilldown-bundle`、`/internal/judge/evidence-claim/ops-queue` 编排均下沉到 `case_read_routes.py` 并接入 `_run_case_read_route_guard` | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_case_read_routes.py tests/test_app_factory.py -k "claim_ledger or courtroom_read_model or courtroom or drilldown or evidence_claim_ops_queue"` |
| `ai-judge-p33-panel-review-alert-route-orchestrator-v2` | P0 | 已完成（2026-04-22） | 下沉 `panel/review/alert/outbox/rag diagnostics` | `panel runtime profiles/readiness` 参数归一化与构建下沉到 `panel_runtime_routes.py`；`alerts/outbox` 与 `rag diagnostics` 编排下沉到 `review_alert_routes.py`；`app_factory` 路由保持 guard + contract 薄层 | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_app_factory.py -k "panel_runtime or review or alert or outbox or rag"` + `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_panel_runtime_routes.py tests/test_review_alert_routes.py` |
| `ai-judge-p33-registry-governance-route-orchestrator-v2` | P1 | 已完成（2026-04-22） | 收口 registry read/governance 依赖装配噪音 | registry governance 路由统一走 `RegistryGovernanceRouteDependencyPack`，`app_factory` 去除重复 default version/lambda 装配；依赖健康、gate simulation、governance overview、prompt-tool governance、domain families 路由均改为 pack 驱动且保持 `_run_registry_route_guard` | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_registry_governance_routes.py tests/test_app_factory.py -k "policy_registry_dependency_health_route or policy_gate_simulation_route or registry_governance"` |
| `ai-judge-p33-agent-advisory-boundary-hardening-v2` | P1 | 已完成（2026-04-22） | 补强 NPC/Room QA 的 advisory-only 断言 | 在 `assistant_agent_routes` 增加 `sanitize_assistant_advisory_output`，阻断官方裁决链字段（含嵌套字段）透传；`app_factory` 的 assistant 响应构建接入净化器；新增应用层与路由层测试覆盖 `winner/verdictReason/needsDrawVote/dimensionScores` 等字段剔除 | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_assistant_agent_routes.py tests/test_agent_runtime.py tests/test_app_factory.py -k "npc or room_qa or advisory or sanitize"` |
| `ai-judge-p33-six-object-read-contract-hardening-v1` | P1 | 已完成（2026-04-22） | 强化 read 面六对象合同稳定性 | `case_overview/courtroom_read_model/review_queue` 合同新增六对象结构键约束：`caseEvidence` 必需含 `caseDossier/claimGraph/evidenceLedger/verdictLedger/fairnessSummary/opinionPack`，`courtroom` 必需含 `recorder/claim/evidence/panel/fairness/opinion/governance`，`drilldown/courtroomSummary` 必需含稳定章节键；同步补齐缺失键失败用例 | `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_case_overview_contract.py tests/test_courtroom_read_model_contract.py tests/test_review_queue_contract.py tests/test_app_factory.py -k "contract or drilldown or evidence_claim"` |
| `ai-judge-p33-local-regression-bundle-v2` | P2 | 已完成（2026-04-22） | 固化 P33 本地回归工件 | `ruff + pytest -q + runtime_ops_pack(--allow-local-reference)` 全通过并产出工件（`ai_judge_runtime_ops_pack_status=local_reference_ready`） | `cd ai_judge_service && ../scripts/py -m ruff check app tests` + `cd ai_judge_service && ../scripts/py -m pytest -q` + `bash scripts/harness/ai_judge_runtime_ops_pack.sh --root /Users/panyihang/Documents/EchoIsle --allow-local-reference` |
| `ai-judge-p33-enterprise-consistency-refresh-v2` | P2 | 已完成（2026-04-22） | 同步企业方案/架构方案/完成度映射/当前计划口径 | 修复计划文档三级标题门禁后，`harness_docs_lint` 与 `ai_judge_plan_consistency_gate` 均通过 | `bash scripts/quality/harness_docs_lint.sh` + `bash scripts/harness/ai_judge_plan_consistency_gate.sh --root /Users/panyihang/Documents/EchoIsle` |
| `ai-judge-p33-real-env-pass-window-execute-on-env` | P0（环境阻塞） | 阻塞（待真实环境） | 完成真实环境 pass 收口 | `AI_JUDGE_REAL_ENV_WINDOW_CLOSURE_STATUS=pass` 且 `REAL_PASS_READY=true` | `bash scripts/harness/ai_judge_real_env_window_closure.sh --root /Users/panyihang/Documents/EchoIsle` |
| `ai-judge-p33-stage-closure-execute` | P2 | 待执行 | P33 阶段收口 | 归档活动计划并同步 `completed.md/todo.md` 最小必要快照 | `bash scripts/harness/ai_judge_stage_closure_execute.sh --root /Users/panyihang/Documents/EchoIsle` |

### 下一开发模块建议

1. `ai-judge-p33-app-factory-hotspot-split-v4`
2. `ai-judge-p33-real-env-pass-window-execute-on-env`
3. `ai-judge-p33-stage-closure-execute`

---

## 5. `app_factory` 剩余热点清单（P33 输入基线）

1. `judge command`：
   - 已基本完成下沉（dispatch 主链语义段已迁移）。
   - 仍需做少量 route 装配收口与统一依赖包。
2. `judge read/courtroom`：
   - 已完成：`/internal/judge/cases/{case_id}`、`/internal/judge/cases/{case_id}/claim-ledger`、`/internal/judge/cases/{case_id}/courtroom-read-model`、`/internal/judge/courtroom/cases`、`/internal/judge/courtroom/drilldown-bundle`、`/internal/judge/evidence-claim/ops-queue`。
   - 当前该子链路已收口，后续以 contract 与回归为主。
3. `panel/review/alert`：
   - 已完成下沉：`/internal/judge/panels/runtime/profiles`、`/internal/judge/panels/runtime/readiness`、`/internal/judge/review/cases`、`/internal/judge/review/cases/{case_id}`、`/internal/judge/alerts/ops-view`、`/internal/judge/alerts/outbox`。
   - 当前该子链路已收口，后续以 contract 与回归为主。
4. 其他：
   - `/internal/judge/rag/diagnostics` 已下沉到 `review_alert_routes.py`。
   - NPC/Room QA advisory-only 边界断言已补强，后续仅保留回归维护

---

## 6. 执行顺序（建议）

1. 先收口 `app_factory hotspot split` 剩余条目（结构性下沉不改语义）。
2. 无真实环境时保持 `real-env pass window` 为 `on-env`，不宣称 `pass`。
3. 当仅剩 `on-env` 阻塞时，执行本阶段收口。

---

## 7. 延后事项（不阻塞 P33）

1. 真实环境窗口验收（严格 `on-env`）。
2. NPC/Room QA 的完整产品策略冻结前，不做业务能力扩展。
3. 协议化扩展（链上锚定 / ZK / ZKML）继续后置，不进入当前官方裁决主链。

---

## 8. 本阶段明确不做

1. 不新增未冻结 PRD 的 NPC/Room QA 新业务功能。
2. 不为未上线能力保留长期兼容层（alias/双写/灰度并行）。
3. 不把本地验证结果表述为真实环境 `pass`。

---

## 9. 验收命令基线

1. `bash skills/python-venv-guard/scripts/assert_venv.sh --project /Users/panyihang/Documents/EchoIsle/ai_judge_service --venv /Users/panyihang/Documents/EchoIsle/ai_judge_service/.venv`
2. `cd ai_judge_service && ../scripts/py -m ruff check app tests`
3. `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_app_factory.py`
4. `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_case_read_routes.py tests/test_judge_command_routes.py tests/test_registry_governance_routes.py`
5. `cd ai_judge_service && ../scripts/py -m pytest -q tests/test_agent_runtime.py tests/test_ops_read_model_pack.py`
6. `cd ai_judge_service && ../scripts/py -m pytest -q`
7. `bash scripts/harness/ai_judge_runtime_ops_pack.sh --root /Users/panyihang/Documents/EchoIsle --allow-local-reference`
8. `bash scripts/quality/harness_docs_lint.sh`
9. `bash scripts/harness/ai_judge_plan_consistency_gate.sh --root /Users/panyihang/Documents/EchoIsle`

---

## 10. 风险与对策

1. 风险：读取链路下沉引发字段缺失或契约漂移。  
   对策：保持 contract 校验不降级，违例继续 500 并补针对性测试。
2. 风险：panel/review/alert 聚合编排迁移导致排序/分页行为变化。  
   对策：迁移前固定 `test_app_factory.py` 行为断言，迁移后逐项对齐。
3. 风险：NPC/Room QA 与主裁决链产生耦合。  
   对策：增加禁止写官方账本字段断言，保持 advisory-only。
4. 风险：计划与代码状态不一致。  
   对策：每批完成后回写计划并执行 `ai_judge_plan_consistency_gate`。
5. 风险：无真实环境导致收口口径混淆。  
   对策：统一 `local_reference_ready` + `on-env` 双层表达。

---

## 11. 同步历史

### 模块完成同步历史

1. 2026-04-22：按要求归档上一阶段计划到 `/Users/panyihang/Documents/EchoIsle/docs/dev_plan/archive/20260422T053246Z-ai-judge-current-plan-archive.md`。
2. 2026-04-22：完成 `ai-judge-next-iteration-planning`，当前计划切换到 `P33`，明确承接企业方案 + 架构方案与现有代码状态的剩余收口项。
3. 2026-04-22：推进 `ai-judge-p33-judge-read-route-orchestrator-v2` 第一批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_read_routes.py` 新增 `build_case_claim_ledger_route_payload`，将 `GET /internal/judge/cases/{case_id}/claim-ledger` 的 dispatch_type 归一化、claim-ledger 查询选择与响应组装编排从 `app_factory` 下沉到 applications 层；`app_factory` 该路由改为 `_run_case_read_route_guard + builder`，并通过定向 `pytest` 与 `ruff` 回归。
4. 2026-04-22：推进 `ai-judge-p33-judge-read-route-orchestrator-v2` 第二批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_read_routes.py` 新增 `build_case_courtroom_read_model_route_payload`，将 `GET /internal/judge/cases/{case_id}/courtroom-read-model` 的 report context 解析、workflow/trace/alert 聚合、courtroom payload 组装编排从 `app_factory` 下沉到 applications 层；`app_factory` 该路由改为 `_run_case_read_route_guard + builder`，保持 contract 校验与错误语义不变，并通过定向 `pytest` 与 `ruff` 回归。
5. 2026-04-22：推进 `ai-judge-p33-judge-read-route-orchestrator-v2` 第三批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_read_routes.py` 新增 `build_case_courtroom_cases_route_payload`，将 `GET /internal/judge/courtroom/cases` 的查询参数归一化、workflow 扫描过滤、report context 错误聚合、risk/callback/courtroomSummary 组装与分页排序编排从 `app_factory` 下沉到 applications 层；`app_factory` 该路由改为 `_run_case_read_route_guard + builder`，保持过滤语义与错误码合同不变，并通过定向 `pytest` 与 `ruff` 回归。
6. 2026-04-22：推进 `ai-judge-p33-judge-read-route-orchestrator-v2` 第四批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_read_routes.py` 新增 `build_case_courtroom_drilldown_bundle_route_payload`，将 `GET /internal/judge/courtroom/drilldown-bundle` 的过滤参数归一化、report context 聚合、drilldown/action hints 与 aggregations 组装编排从 `app_factory` 下沉到 applications 层；`app_factory` 该路由改为 `_run_case_read_route_guard + builder + contract` 薄层，并通过定向 `pytest` 与 `ruff` 回归。
7. 2026-04-22：推进 `ai-judge-p33-judge-read-route-orchestrator-v2` 第五批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_read_routes.py` 新增 `build_case_evidence_claim_ops_queue_route_payload`，将 `GET /internal/judge/evidence-claim/ops-queue` 的多维过滤、risk/reliability 聚合、ops profile/action hints 与 aggregations 编排从 `app_factory` 下沉到 applications 层；`app_factory` 该路由改为 `_run_case_read_route_guard + builder + contract` 薄层，并通过定向 `pytest` 与 `ruff` 回归。
8. 2026-04-22：推进 `ai-judge-p33-panel-review-alert-route-orchestrator-v2`，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/panel_runtime_routes.py` 新增 `PanelRuntimeRouteError` 与 route-level builder（`build_panel_runtime_profiles_route_payload`、`build_panel_runtime_readiness_route_payload`），将 `panel runtime profiles/readiness` 的参数归一化与边界校验从 `app_factory` 下沉到 applications 层；同时在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/review_alert_routes.py` 新增 `build_alert_outbox_route_payload`、`build_rag_diagnostics_payload`，将 `alerts/outbox` 与 `rag diagnostics` 编排下沉；`app_factory` 对应路由收敛为 guard + builder + contract 薄层，并通过定向 `pytest` 与 `ruff` 回归。
9. 2026-04-22：推进 `ai-judge-p33-registry-governance-route-orchestrator-v2`，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_governance_routes.py` 新增 `RegistryGovernanceRouteDependencyPack` 与 from-pack builder（dependency health/gate simulation/governance overview/prompt-tool governance/domain families），统一 registry governance 依赖注入；`app_factory` 新增 `_build_registry_governance_route_dependency_pack` 并改为 pack 驱动调用，去除重复 default version/lambda 装配噪音，保持 `_run_registry_route_guard` 与错误语义不变，并通过定向 `pytest` 与 `ruff` 回归。
10. 2026-04-22：推进 `ai-judge-p33-agent-advisory-boundary-hardening-v2`，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/assistant_agent_routes.py` 新增 `sanitize_assistant_advisory_output`，统一剔除 assistant 输出中的官方裁决链字段（含嵌套 dict/list 场景）；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_build_assistant_agent_response` 接入该净化器，防止 NPC/Room QA 输出污染官方 verdict/fairness/trust 主链；并新增 `test_assistant_agent_routes.py` 与 `test_app_factory.py` 断言覆盖后通过 `ruff + pytest` 定向回归。
11. 2026-04-22：推进 `ai-judge-p33-six-object-read-contract-hardening-v1`，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_overview_contract.py`、`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/courtroom_read_model_contract.py`、`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/review_queue_contract.py` 增加六对象映射结构键约束，防止 read 面退化为散字段拼接；同步更新 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_case_overview_contract.py`、`/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_courtroom_read_model_contract.py`、`/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_review_queue_contract.py` 并新增缺失键失败用例，定向 `ruff + pytest` 回归通过。
12. 2026-04-22：推进 `ai-judge-p33-local-regression-bundle-v2`，完成 `ruff check app tests` 与 `pytest -q` 全量本地回归，并执行 `ai_judge_runtime_ops_pack.sh --allow-local-reference`，结果为 `ai_judge_runtime_ops_pack_status=local_reference_ready`，工件写入 `/Users/panyihang/Documents/EchoIsle/artifacts/harness/20260422T082110Z-ai-judge-runtime-ops-pack.summary.{json,md}`。
13. 2026-04-22：推进 `ai-judge-p33-enterprise-consistency-refresh-v2`，修复 `/Users/panyihang/Documents/EchoIsle/docs/dev_plan/当前开发计划.md` 缺失 `### 模块完成同步历史` 标题导致的文档门禁失败后，`harness_docs_lint` 与 `ai_judge_plan_consistency_gate` 均恢复通过。
14. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 1 子批，新增 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_courtroom_views.py`，将 `case_evidence` 与 `evidence_claim` 相关构建函数（`build_case_evidence_view`、`build_evidence_claim_reliability_profile`、`build_evidence_claim_ops_profile`、`build_evidence_claim_action_hints`、`build_evidence_claim_queue_sort_key`）从 `app_factory` 下沉到 applications 层；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 改为薄包装调用，保持行为不变，并通过 `ruff + pytest -q` 全量回归。
15. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 2 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_courtroom_views.py` 继续下沉 `build_courtroom_read_model_view`、`build_courtroom_read_model_light_summary`、`build_courtroom_drilldown_bundle_view`、`build_courtroom_drilldown_action_hints`；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应函数统一改为薄包装调用（通过 `normalize_fairness_gate_decision` 注入保持语义一致），并通过定向与全量 `pytest -q` 回归。
16. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 3 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/case_courtroom_views.py` 新增 `serialize_claim_ledger_record`、`normalize_courtroom_case_sort_by/order`、`build_courtroom_case_sort_key`、`normalize_evidence_claim_reliability_level`、`normalize_evidence_claim_queue_sort_by/order`，将 `claim_ledger` 序列化与 `courtroom/evidence_claim` 查询归一化/排序工具从 `app_factory` 下沉到 applications 层；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应函数统一改为薄包装调用，并通过 `ruff + pytest` 定向回归（含 replay claim_ledger 用例）通过。
17. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 4 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/trust_ops_views.py` 新增 `trust_challenge` 过滤归一化与优先级/排序工具函数（`normalize_trust_challenge_*`、`build_trust_challenge_priority_profile`、`build_trust_challenge_sort_key`、`build_trust_challenge_action_hints`、`build_review_trust_unified_priority_profile`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用（保持路由装配签名与语义不变）；定向 `pytest`（`trust_challenge_ops_queue + review_cases`）与 `ruff` 回归通过。
18. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 5 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/review_alert_routes.py` 新增 `review_case` 过滤归一化与风险画像/排序工具函数（`normalize_review_case_risk_level`、`normalize_review_case_sla_bucket`、`normalize_review_case_sort_by/order`、`build_review_case_risk_profile`、`build_review_case_sort_key`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用（通过 `normalize_query_datetime` 回调保持时序语义一致）；定向 `pytest`（`review_cases + case_read`）与 `ruff` 回归通过。
19. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 6 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/fairness_runtime_routes.py` 新增 `case_fairness` 过滤归一化与排序/聚合工具函数（`normalize_case_fairness_gate_conclusion`、`normalize_case_fairness_challenge_state`、`normalize_case_fairness_sort_by/order`、`build_case_fairness_sort_key`、`build_case_fairness_aggregations`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用（通过 `CASE_FAIRNESS_GATE_CONCLUSIONS` 注入保持统计语义一致）；定向 `pytest`（`fairness case list + panel runtime`）与 `ruff` 回归通过。
20. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 7 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/panel_runtime_routes.py` 新增 `panel_runtime` 过滤归一化与列表/就绪度构建工具函数（`normalize_panel_runtime_profile_source`、`normalize_panel_runtime_profile_sort_by/order`、`build_panel_runtime_profile_item`、`build_panel_runtime_profile_sort_key`、`build_panel_runtime_profile_aggregations`、`build_panel_runtime_readiness_summary`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用；定向 `pytest`（`panel_runtime profile/readiness`）与 `ruff` 回归通过。
21. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 8 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_ops_views.py` 新增 `registry alert ops` 过滤归一化与 outbox 索引工具函数对外导出（`normalize_ops_alert_status`、`normalize_ops_alert_delivery_status`、`normalize_ops_alert_fields_mode`、`normalize_registry_audit_action`、`build_alert_outbox_index`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用；定向 `pytest`（`registry_alert_ops_view + policy_registry_audits_route`）与 `ruff` 回归通过。
22. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 9 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_ops_views.py` 新增 `registry dependency` 过滤归一化与概览/趋势构建工具函数对外导出（`normalize_registry_dependency_trend_status`、`build_registry_dependency_overview`、`build_registry_dependency_trend`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用；定向 `pytest`（`policy_registry_dependency_health_route + registry_alert_ops_view + policy_registry_audits_route`）与 `ruff` 回归通过。
23. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 10 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_ops_views.py` 新增 `registry prompt-tool` 风险构建工具函数对外导出（`registry_prompt_tool_risk_severity_rank`、`build_registry_prompt_tool_usage_rows`、`build_registry_prompt_tool_risk_items`、`build_registry_prompt_tool_action_hints`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用；定向 `pytest`（`prompt_tool_governance + policy_registry_dependency_health_route + registry_alert_ops_view + policy_registry_audits_route`）与 `ruff` 回归通过。
24. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 11 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_ops_views.py` 新增 `registry_alert` 趋势/序列化/审计关联索引工具函数对外导出（`build_registry_alert_ops_trend`、`serialize_registry_alert_ops_item`、`build_registry_alert_link_index_for_audits`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应实现替换为薄包装调用；定向 `pytest`（`prompt_tool_governance + policy_registry_dependency_health_route + registry_alert_ops_view + policy_registry_audits_route`）与 `ruff` 回归通过。
25. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 12 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/fairness_runtime_routes.py` 新增 `case_fairness` 条目构建函数对外导出（`build_case_fairness_item`）并下沉 challenge snapshot 解析逻辑；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_build_case_fairness_item` 改为薄包装调用并保留 `_build_judge_core_view` 回调语义不变；定向 `pytest`（`fairness_case_list + fairness_case_detail + fairness_dashboard`）与 `ruff` 回归通过。
26. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 13 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/fairness_runtime_routes.py` 新增 `fairness calibration` helper 对外导出（`build_fairness_calibration_on_env_input_template`、`build_fairness_policy_calibration_recommended_actions`）；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 对应函数改为薄包装调用，并清理已无引用的阈值建议辅助函数（`_parse_iso_datetime/_optional_float/_optional_int/_pick_threshold_value/_suggest_max_threshold/_suggest_sample_size_floor`）；定向 `pytest`（`fairness_calibration_pack + policy_calibration_advisor`）与 `ruff` 回归通过。
27. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 14 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 `blindization` 敏感键扫描函数导出（`find_sensitive_key_hits`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_find_sensitive_key_hits` 改为薄包装调用，同时移除 `app_factory` 内部重复实现与 `_BLIND_SENSITIVE_KEY_TOKENS` 常量；定向 `pytest`（`blindization_rejection` 路由链路）与 `ruff` 回归通过。
28. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 15 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增错误合同与失败回调构建函数导出（`build_error_contract`、`with_error_contract`、`build_failed_callback_payload`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_build_error_contract`、`_with_error_contract`、`_build_failed_callback_payload` 改为薄包装调用，同时移除 `app_factory` 内部重复敏感键扫描实现；定向 `pytest`（`blindization_reject/blindization_rejection` 路由链路）与 `ruff` 回归通过。
29. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 16 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 `blindization` 元数据与回执维度构建函数导出（`build_dispatch_meta_from_raw`、`build_receipt_dims_from_raw`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_extract_dispatch_meta_from_raw`、`_extract_receipt_dims_from_raw` 改为薄包装调用；定向 `pytest`（`blindization_reject/blindization_rejection` 路由链路）与 `ruff` 回归通过。
30. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 17 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 `idempotency` 解析、dispatch request 校验与 callback 函数选择 helper 导出（`resolve_idempotency_or_raise`、`validate_phase_dispatch_request`、`validate_final_dispatch_request`、`resolve_failed_callback_fn_for_dispatch`、`resolve_report_callback_fn_for_dispatch`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_resolve_idempotency_or_raise`、`_validate_phase_dispatch_request`、`_validate_final_dispatch_request`、`_failed_callback_fn_for_dispatch`、`_report_callback_fn_for_dispatch` 改为薄包装调用；补充应用层 helper 单测并通过 `ruff + pytest` 回归。
31. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 18 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增原始 payload 提取 helper 导出（`extract_raw_field`、`extract_optional_int/float/str/bool/datetime`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_extract_raw_field`、`_extract_optional_int/float/str/bool/datetime` 改为薄包装调用（`datetime` 保持 `_normalize_query_datetime` 归一化语义）；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
32. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 19 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 callback 重试与 dispatch receipt 落库 helper 导出（`invoke_callback_with_retry`、`save_dispatch_receipt`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_invoke_v3_callback_with_retry`、`_save_dispatch_receipt` 改为薄包装调用；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
33. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 20 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 `safe_float` 与 `resolve_winner` helper 导出，并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_safe_float`、`_resolve_winner` 改为薄包装调用；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
34. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 21 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 final report payload 构建 helper 导出（`build_final_report_payload_for_dispatch`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_build_final_report_payload` 改为薄包装调用；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
35. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 22 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 registry profile resolver helper 导出（`resolve_policy_profile_or_raise`、`resolve_prompt_profile_or_raise`、`resolve_tool_profile_or_raise`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_resolve_policy_profile_or_raise`、`_resolve_prompt_profile_or_raise`、`_resolve_tool_profile_or_raise` 改为薄包装调用（保留 `HTTPException` 错误语义）；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
36. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 23 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 policy trace snapshot helper 导出（`attach_policy_trace_snapshot`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_attach_policy_trace_snapshot` 改为薄包装调用（保留原有 payload 判定与 registryVersions 语义）；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
37. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 24 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 panel runtime profile resolver helper 导出（`resolve_panel_runtime_profiles`），并将 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 的 `_resolve_panel_runtime_profiles` 改为薄包装调用（保留 camel/snake 元数据兼容、topic_domain 回退与 profileSource 语义）；补充应用层 helper 单测并通过 `ruff + pytest` 定向回归。
38. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 25 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_trace_report_summary`、`_build_phase_judge_workflow_payload`、`_build_final_judge_workflow_payload` 三个纯转发 helper，并将 `case-create/phase-dispatch/final-dispatch` 路由编排回调改为直接接线 applications 层函数（`build_trace_report_summary_v3`、`build_phase_judge_workflow_payload_v3`、`build_final_judge_workflow_payload_v3`）；通过 `ruff + pytest` 定向回归确认语义不变。
39. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 26 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_attach_report_attestation` 纯转发 helper，并将 `phase/final dispatch` 与 `replay report deps` 的 `attach_report_attestation` 回调改为直接接线 applications 层函数 `attach_report_attestation_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
40. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 27 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_serialize_prompt_profile`、`_serialize_tool_profile`、`_ensure_registry_runtime_loaded` 三个纯转发 helper，并将 prompt/tool registry 路由序列化与 registry runtime ready 流程改为直接接线 `runtime.prompt_registry_runtime.serialize_profile`、`runtime.tool_registry_runtime.serialize_profile` 与 `runtime.registry_product_runtime.ensure_loaded`；通过 `ruff + pytest` 定向回归确认语义不变。
41. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 28 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_final_report_payload` 纯转发 helper，并将 final dispatch 报告构建与 replay 报告构建改为直接接线 applications 层函数 `build_final_report_payload_for_dispatch_v3`（保留 `list_dispatch_receipts/build_final_report_payload/judge_style_mode` 注入语义）；通过 `ruff + pytest` 定向回归确认语义不变。
42. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 29 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_serialize_alert_item`、`_serialize_outbox_event`、`_serialize_dispatch_receipt` 三个纯转发 helper，并将 alerts/outbox/dispatch receipt 相关路由与编排改为直接接线 applications 层函数 `serialize_alert_item_v3`、`serialize_outbox_event_v3`、`serialize_dispatch_receipt_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
43. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 30 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_verdict_contract` 纯转发 helper，并将 case-overview/courtroom/replay 依赖注入中的 `build_verdict_contract` 全部改为直接接线 `build_verdict_contract_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
44. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 31 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_case_evidence_view` 纯转发 helper，并将 case-overview/courtroom/evidence-claim 依赖注入中的 `build_case_evidence_view` 全部改为直接接线 `build_case_evidence_view_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
45. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 32 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_serialize_claim_ledger_record` 纯转发 helper，并将 claim-ledger/replay-report 路由依赖注入改为直接接线 `serialize_claim_ledger_record_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
46. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 33 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_safe_float`、`_resolve_winner` 纯转发 helper，并将 replay finalize 依赖注入改为直接接线 `safe_float_v3`、`resolve_winner_v3`；同步修复 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_app_factory.py` 中 final payload patch 目标为 `build_final_report_payload_for_dispatch_v3` 以匹配第 28 子批后的主链实现，并通过 `ruff + pytest` 定向回归。
47. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 34 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_error_contract`、`_build_failed_callback_payload`、`_with_error_contract` 三个纯转发 helper，并将 workflow failed、phase/final blindization rejection、final contract blocked、phase/final callback result 统一改为直接接线 `build_error_contract_v3`、`build_failed_callback_payload_v3`、`with_error_contract_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
48. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 35 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_find_sensitive_key_hits`、`_extract_dispatch_meta_from_raw`、`_extract_receipt_dims_from_raw` 三个纯转发 helper，并将 phase/final blindization rejection 的敏感键扫描与 dispatch meta/receipt dims 解析改为直接接线 `find_sensitive_key_hits_v3`、`build_dispatch_meta_from_raw_v3`、`build_receipt_dims_from_raw_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
49. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 36 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_resolve_idempotency_or_raise` 纯转发 helper，并将 case-create/phase-preflight/final-preflight 的幂等解析改为直接接线 `resolve_idempotency_or_raise_v3`（保留 `resolve_idempotency` 与 `ttl_secs` 注入语义）；通过 `ruff + pytest` 定向回归确认语义不变。
50. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 37 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_extract_raw_field`、`_extract_optional_int`、`_extract_optional_float`、`_extract_optional_str`、`_extract_optional_bool`、`_extract_optional_datetime` 六个纯转发 helper，并将 registry publish 与 fairness benchmark/shadow upsert 的解析依赖改为直接接线 `extract_optional_*_v3`（`datetime` 保持 `normalize_query_datetime` 注入语义）；通过 `ruff + pytest` 定向回归确认语义不变。
51. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 38 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_courtroom_case_sort_by`、`_normalize_courtroom_case_sort_order`、`_normalize_evidence_claim_reliability_level`、`_normalize_evidence_claim_queue_sort_by`、`_normalize_evidence_claim_queue_sort_order` 五个纯转发 helper，并将 `courtroom cases/drilldown` 与 `evidence_claim ops` 的排序/过滤归一化依赖改为直接接线对应 `*_v3` 函数；通过 `ruff + pytest` 定向回归确认语义不变。
52. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 39 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_review_case_risk_level`、`_normalize_review_case_sla_bucket`、`_normalize_review_case_sort_by`、`_normalize_review_case_sort_order` 四个纯转发 helper，并将 `courtroom/review` 过滤与排序归一化依赖改为直接接线 `normalize_review_case_*_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
53. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 40 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_trust_challenge_state_filter`、`_normalize_trust_challenge_review_state`、`_normalize_trust_challenge_priority_level`、`_normalize_trust_challenge_sla_bucket`、`_normalize_trust_challenge_sort_by`、`_normalize_trust_challenge_sort_order` 六个纯转发 helper，并将 trust challenge 队列与 review 关联过滤归一化改为直接接线 `normalize_trust_challenge_*_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
54. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 41 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_case_fairness_gate_conclusion`、`_normalize_case_fairness_challenge_state`、`_normalize_case_fairness_sort_by`、`_normalize_case_fairness_sort_order` 四个纯转发 helper，并将 fairness case list + panel runtime + review 关联过滤归一化改为直接接线 `normalize_case_fairness_*_v3`；同步修复 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_app_factory.py` 中 `build_final_report_payload_for_dispatch_v3` patch side-effect 的签名以兼容当前注入参数，最终通过 `ruff + pytest` 定向回归。
55. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 42 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_panel_runtime_profile_source`、`_normalize_panel_runtime_profile_sort_by`、`_normalize_panel_runtime_profile_sort_order` 三个纯转发 helper，并将 panel runtime profiles/readiness 过滤归一化依赖改为直接接线 `normalize_panel_runtime_profile_*_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
56. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 43 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_normalize_registry_dependency_trend_status`、`_normalize_ops_alert_status`、`_normalize_ops_alert_delivery_status`、`_normalize_ops_alert_fields_mode`、`_normalize_registry_audit_action` 五个纯转发 helper，并将 registry governance + alert ops + registry audits 的归一化依赖改为直接接线对应 `*_v3` 函数；通过 `ruff + pytest` 定向回归确认语义不变。
57. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 44 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_courtroom_case_sort_key`、`_build_evidence_claim_queue_sort_key`、`_build_trust_challenge_sort_key`、`_build_review_case_sort_key`、`_build_case_fairness_sort_key` 五个纯转发 helper，并将 courtroom/evidence/review/trust/fairness 列表排序依赖改为直接接线 `build_*_sort_key_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
58. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 45 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_panel_runtime_profile_sort_key`、`_build_panel_runtime_profile_aggregations`、`_build_panel_runtime_readiness_summary` 三个纯转发 helper，并将 panel runtime profiles/readiness 构建链路改为直接接线 `build_panel_runtime_profile_sort_key_v3`、`build_panel_runtime_profile_aggregations_v3`、`build_panel_runtime_readiness_summary_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
59. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 46 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_courtroom_drilldown_action_hints`、`_build_evidence_claim_ops_profile`、`_build_evidence_claim_action_hints` 三个纯转发 helper，并将 `courtroom_drilldown_bundle` 与 `evidence_claim_ops_queue` 路由依赖注入改为直接接线 `build_courtroom_drilldown_action_hints_v3`、`build_evidence_claim_ops_profile_v3`、`build_evidence_claim_action_hints_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
60. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 47 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_fairness_dashboard_case_trends`、`_build_fairness_dashboard_run_trends`、`_build_fairness_dashboard_top_risk_cases`、`_build_fairness_calibration_threshold_suggestions`、`_build_fairness_calibration_drift_summary`、`_build_fairness_calibration_risk_items`、`_build_fairness_calibration_on_env_input_template`、`_build_fairness_policy_calibration_recommended_actions` 八个纯转发 helper，并将 fairness dashboard/calibration/advisor 路由依赖注入改为直接接线对应 `*_v3` 构建函数；通过 `ruff + pytest` 定向回归确认语义不变。
61. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 48 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_registry_dependency_overview`、`_build_registry_dependency_trend`、`_build_registry_prompt_tool_usage_rows`、`_build_registry_prompt_tool_risk_items`、`_build_registry_prompt_tool_action_hints` 五个纯转发 helper，并将 registry governance dependency/prompt-tool pack 注入改为直接接线 `build_registry_dependency_overview_v3`、`build_registry_dependency_trend_v3`、`build_registry_prompt_tool_usage_rows_v3`、`build_registry_prompt_tool_risk_items_v3`、`build_registry_prompt_tool_action_hints_v3`；通过 `ruff + pytest` 定向回归确认语义不变。
62. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 49 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_registry_audit_ops_view`、`_build_registry_alert_ops_view`、`_build_panel_runtime_profile_item` 三个纯转发 helper，并同步清理已无引用的 `_normalize_evidence_claim_reliability_counts`、`_build_evidence_claim_reliability_profile`、`_registry_prompt_tool_risk_severity_rank`、`_build_alert_outbox_index`、`_build_registry_alert_ops_trend`、`_serialize_registry_alert_ops_item`、`_build_registry_alert_link_index_for_audits`；对应路由依赖注入改为直接接线 `build_registry_audit_ops_view_v3`、`build_registry_alert_ops_view_v3`、`build_panel_runtime_profile_item_v3`，并清理未使用 import；通过 `ruff + pytest` 定向回归确认语义不变。
63. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 50 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 read/trust/fairness/panel 相关 contract 校验纯转发 helper（`_validate_case_overview_contract`、`_validate_courtroom_read_model_contract`、`_validate_courtroom_drilldown_bundle_contract`、`_validate_evidence_claim_ops_queue_contract`、`_validate_trust_*_contract`、`_validate_case_fairness_*_contract`、`_validate_fairness_dashboard_contract`、`_validate_panel_runtime_profile_contract`），路由改为直接接线 `validate_*_contract_v3`；同步更新 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/tests/test_app_factory.py` 的 patch 目标到新主链符号并通过 `ruff + pytest` 回归。
64. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 51 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 replay report dependency 里的六个纯转发 helper（policy/prompt/tool resolver、final report payload builder、agent runtime trace attach、policy trace snapshot attach），并在 `ReplayReportDependencyPack_v3` 内联 lambda 直连 `_resolve_*_or_raise`、`build_final_report_payload_for_dispatch_v3` 与 `_attach_*` 主链；通过 `ruff + pytest` 定向回归确认语义不变。
65. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 52 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_replay_context_dependencies`、`_build_replay_report_dependencies`、`_build_replay_finalize_dependencies` 三个 pack builder helper，并将 replay 路由改为直接内联 `Replay*DependencyPack_v3` 组装；通过 `ruff + pytest` 组合回归（`test_app_factory + case_read + judge_trace_replay + review_alert + panel_runtime + registry_governance + ops_read_model_pack`）确认语义不变。
66. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 53 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_failed_callback_fn_for_dispatch`、`_report_callback_fn_for_dispatch`、`_save_dispatch_receipt`、`_validate_final_report_payload_contract` 四个纯转发 helper，并将 blindization/callback-delivery/final-contract-blocked/replay 依赖改为直接接线 `resolve_*_callback_fn_for_dispatch_v3`、`save_dispatch_receipt_v3`、`validate_final_report_payload_contract_v3_final`；通过 `ruff + pytest` 组合回归（`test_app_factory + case_read + judge_trace_replay + review_alert + panel_runtime + registry_governance + ops_read_model_pack`）确认语义不变。
67. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 54 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_resolve_policy_profile_or_raise`、`_resolve_prompt_profile_or_raise`、`_resolve_tool_profile_or_raise` 三个纯转发 helper，并将 case-create/phase-preflight/final-preflight/replay report deps 的 resolver 依赖改为直接接线 `resolve_*_profile_or_raise_v3`（显式注入 `runtime.*_registry_runtime` 解析器）；通过 `ruff + pytest` 组合回归确认语义不变。
68. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 55 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_attach_policy_trace_snapshot` 纯转发 helper，并将 phase/final dispatch 及 replay report deps 的 policy trace 注入改为直接接线 `attach_policy_trace_snapshot_v3`（显式注入 policy/prompt/tool trace snapshot builder）；通过 `ruff + pytest` 组合回归确认语义不变。
69. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 56 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_payload_int`、`_payload_str` 顶层 helper，将 payload 解析逻辑内聚到 `_build_case_dossier_from_request_payload` 内部局部函数，减少全局 helper 暴露面并保持 case dossier 构建语义不变；通过 `ruff + pytest` 组合回归确认语义不变。
70. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 57 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 修复 policy profile domain-family 序列化回调签名兼容性，将 `_serialize_policy_profile_with_domain_family` 从仅关键字参数调整为可兼容位置参数调用，消除 `registry_governance` pack 在 `serialize_policy_profile(profile)` 场景下的 `TypeError`；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认。
71. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 58 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/assistant_agent_routes.py` 新增 `build_assistant_agent_response` 并承接 advisory-only 输出净化与 capability boundary 组装；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_assistant_agent_response` 局部 helper，NPC/Room QA 路由改为直接接线 applications 层 builder；通过 `ruff + pytest`（含 `test_assistant_agent_routes + test_app_factory`）回归确认语义不变。
72. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 59 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/registry_governance_routes.py` 新增 `serialize_policy_profile_with_domain_family`，将 policy profile 的 domain-family 元数据增强逻辑下沉到 applications 层；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除同名局部 helper，`/internal/judge/policies`、`/internal/judge/policies/{policy_version}` 与 `RegistryGovernanceRouteDependencyPack` 改为直接接线新函数；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认语义不变。
73. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 60 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/applications/judge_command_routes.py` 新增 `build_workflow_job`，将 workflow job 构建归一化逻辑下沉到 applications 层；`/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 删除 `_build_workflow_job` 局部 helper，并将 `case-create/phase-preflight/final-preflight/blindization` 等链路改为直接接线 `build_workflow_job_v3`；通过 `ruff + pytest`（`test_judge_command_routes + test_app_factory + test_judge_trace_replay_routes + test_registry_governance_routes`）回归确认语义不变。
74. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 61 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_raise_http_422_from_value_error`、`_raise_http_404_from_lookup_error`、`_raise_http_422_for_known_value_error`、`_raise_policy_registry_not_found_lookup_error`、`_raise_http_500_contract_violation`、`_raise_registry_value_error` 与 `_run_trust_read_guard_sync` 从 `create_app` 内部提升为模块级 helper，收敛路由装配函数体长度并保持错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认语义不变。
75. 2026-04-22：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 62 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_build_registry_governance_route_dependency_pack` 从 `create_app` 内部提升为模块级 `_build_registry_governance_route_dependency_pack_for_runtime`，并将 policy dependency health / governance overview / prompt-tool governance / domain families / gate simulation 五个路由改为直接接线新 builder，保持默认版本动态读取与依赖注入语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认语义不变。
76. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 63 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `case_dossier` 构建中的 `_payload_int_for_case_dossier`、`_payload_str_for_case_dossier` 从函数内联局部 helper 提升为模块级 `_payload_int_from_mapping`、`_payload_str_from_mapping`，保持字段解析容错语义不变并减少函数内部噪音；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认语义不变。
77. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 64 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_read_json_object_or_raise_422` 从 `create_app` 内部提升为模块级 async helper，并维持 registry 发布、case-create、phase/final dispatch、fairness upsert 等路由的 JSON 解析与 422 错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes`）回归确认语义不变。
78. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 65 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 registry fairness/dependency 评估与告警相关 helper（`_evaluate_policy_release_fairness_gate`、`_evaluate_policy_registry_dependency_health`、`_emit_registry_fairness_gate_alert`、`_emit_registry_dependency_health_alert`、`_resolve_registry_dependency_health_alerts`）及 `shared_room_context` 构建器从 `create_app` 内部提升为模块级 `*_for_runtime` helper，并在 `create_app` 通过 `partial` 注入；保持 registry 发布/激活门禁、NPC/Room QA shared context 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
79. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 66 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_list_audit_alerts` 与 `_workflow_get_job/_workflow_list_jobs/_workflow_list_events/_workflow_append_event` 从 `create_app` 内部提升为模块级 runtime wrapper（`*_for_runtime`），并以同名 `partial` 绑定保持路由调用点不变，继续缩减 `create_app` 机械包装噪音；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认语义不变。
80. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 67 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_upsert_fairness_benchmark_run/_list_fairness_benchmark_runs/_upsert_fairness_shadow_run/_list_fairness_shadow_runs` 从 `create_app` 内部提升为模块级 runtime wrapper（`*_for_runtime`），并用同名 `partial` 注入保持调用点与错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
81. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 68 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_get_dispatch_receipt/_list_dispatch_receipts/_append_replay_record/_list_replay_records/_get_claim_ledger_record/_list_claim_ledger_records` 从 `create_app` 内部提升为模块级 runtime wrapper（`*_for_runtime`），并以同名 `partial` 绑定保持调用点与 fallback 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
82. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 69 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_sync_audit_alert_to_facts`、`_workflow_register_and_mark_blinded/_workflow_register_and_mark_case_built/_workflow_mark_completed/_workflow_mark_review_required/_workflow_mark_failed/_workflow_mark_replay` 与 `_transition_judge_alert_status` 从 `create_app` 内部提升为模块级 runtime helper（`*_for_runtime`）并用 `partial` 注入，保持 workflow 核心状态推进、alert 状态流转及错误合同语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
83. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 70 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_persist_dispatch_receipt` 与 `_upsert_claim_ledger_record` 从 `create_app` 内部提升为模块级 runtime wrapper（`*_for_runtime`）并通过 `partial` 注入，保持 dispatch receipt 双写（trace_store + facts）及 claim-ledger 聚合/容错语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
84. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 71 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_resolve_report_context_for_case` 与 `_build_trust_phasea_bundle` 从 `create_app` 内部提升为模块级 runtime wrapper（`*_for_runtime`）并通过 `partial` 注入，保持 trust 读链路 guard、上下文缺失错误语义与 phasea bundle 聚合语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
85. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 72 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增模块级 `_build_replay_dependency_packs_for_runtime`，将 `replay_judge_job` 路由内联的 `ReplayContextDependencyPack/ReplayReportDependencyPack/ReplayFinalizeDependencyPack` 装配整体外提，并在 `create_app` 中预构建后复用，保持 replay 主链编排与错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
86. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 73 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_resolve_report_context_for_case`、`_build_trust_phasea_bundle` 两个 trust 链路 `partial` 装配点从中段路由区前移到 `create_app` 统一 bootstrap 区，清理路由中段重复初始化噪音并保持 trust 读链路 guard/合同语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
87. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 74 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_transition_judge_alert_status` 的 `partial` 装配从 review 路由尾段迁移到 `create_app` 统一 bootstrap 区，保持 ack/resolve alert 状态流转与 `_run_review_route_guard` 语义不变并进一步收敛尾段装配噪音；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
88. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 75 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `build_registry_governance_dependency_pack` 统一工厂（`partial(_build_registry_governance_route_dependency_pack_for_runtime, ...)`），将 dependency health/governance overview/prompt-tool governance/domain families/gate simulation 五个路由的重复依赖 pack 组装收敛为单点调用，保持 registry 读链路语义与门禁行为不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
89. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 76 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增模块级 `_ensure_workflow_schema_ready_for_runtime` 与 `_ensure_registry_runtime_ready_for_runtime`，将 `create_app` 内联 readiness async helper 改为 `partial` 绑定（保留 schema lock + once 语义），进一步清理 `create_app` 内部定义噪音并保持 registry/workflow 初始化时序不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
90. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 77 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_registry_profiles_payload_for_runtime` 与 `_build_registry_profile_payload_for_runtime`，将 prompt/tool registry 的 list/get 路由重复 payload 构建逻辑统一为模块级 helper，保持 404 错误语义与 serializer 输出不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
91. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 78 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_validated_trust_item_payload_for_runtime`，将 trust commitment/verdict-attestation/challenge-review/kernel-version 四个路由重复的 bundle + 合同校验装配统一下沉到 helper，保持 trust guard 与 violation code 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
92. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 79 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_trust_audit_anchor_payload_for_runtime` 与 `_build_trust_public_verify_payload_for_runtime`，将 trust audit-anchor/public-verify 路由重复的 phasea bundle 装配与 guard 包裹收敛为统一 helper，保持 include_payload、公验载荷与合同校验语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
93. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 80 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_registry_release_gate_dependencies_for_runtime`，将 registry publish/activate 两条路由共享的 policy dependency + fairness gate 依赖注入统一为公共 dependency pack（`registry_release_gate_dependencies`），保持 registry 发布/激活门禁、错误码与告警语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
94. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 81 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_trust_challenge_common_dependencies_for_runtime`，将 trust challenge request/decision 两条路由共享的 context/workflow/bundle/event 依赖注入统一为公共 dependency pack（`trust_challenge_common_dependencies`），保持 challenge 状态机推进与审计语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
95. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 82 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_review_case_risk_profile_for_runtime`、`_build_trust_challenge_priority_profile_for_runtime`、`_build_trust_challenge_action_hints_for_runtime` 与 `_build_review_trust_unified_priority_profile_for_runtime` 四个模块级 runtime wrapper，统一 review/trust 优先级计算链路中的重复 lambda 包装；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
96. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 83 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 以 `partial` 预绑定方式统一注入 `build_review_case_risk_profile`、`build_trust_challenge_priority_profile`、`build_trust_challenge_action_hints`、`build_review_trust_unified_priority_profile`，并替换 `courtroom/review/trust-challenge` 多路由重复 lambda 传参，保持排序/优先级与合同语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
97. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 84 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_courtroom_read_model_view_for_runtime`、`_build_courtroom_read_model_light_summary_for_runtime`、`_build_courtroom_drilldown_bundle_view_for_runtime` 三个模块级 runtime wrapper，统一 courtroom read-model/drilldown 链路中对 `normalize_fairness_gate_decision` 的重复注入；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
98. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 85 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_extract_optional_datetime_for_runtime`、`_build_case_fairness_item_for_runtime`、`_build_case_fairness_aggregations_for_runtime` 并在 `create_app` 统一注入 `extract_optional_datetime/build_case_fairness_item/build_case_fairness_aggregations`，收敛 fairness 链路重复 lambda 装配点；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
99. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 86 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `case_read`（`courtroom_read_model/courtroom_cases/drilldown_bundle/evidence_claim_ops_queue`）与 `fairness`（benchmark/shadow upsert、case detail/list、dashboard）路由中的重复 lambda 全量替换为共享 runtime wrapper 注入，保持合同校验、排序与门禁语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes`）回归确认。
100. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 87 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_invoke_v3_callback_with_retry_for_runtime`、`_invoke_failed_callback_with_retry_for_runtime`、`_attach_policy_trace_snapshot_for_runtime` 与 `_build_final_report_payload_for_runtime`，并在 `create/phase/final dispatch` 链路统一预绑定 `resolve_*`、callback retry、trace snapshot、dispatch meta/dims 与 final report builder 依赖，替换重复 lambda 注入且保留 `build_final_report_payload_for_dispatch_v3` 的运行时 patch 语义；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
101. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 88 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_build_replay_dependency_packs_for_runtime` 改为接收共享注入参数（`resolve_policy/prompt/tool_profile`、`build_final_report_payload`、`resolve_panel_runtime_profiles`、`build_phase_report_payload`、`attach_judge_agent_runtime_trace`、`attach_policy_trace_snapshot`），并在 `create_app` 将已预绑定依赖直接注入 replay pack builder，去除 pack 内部重复 lambda 装配；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
102. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 89 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `_run_trust_read_guard_sync` 改为支持 `builder + kwargs` 直接调用，并在 trust validated-item/audit-anchor/public-verify 三条 helper 链路去除 lambda 包裹；同时新增 registry governance pack 的共享 wrapper（policy profile serializer、dependency health evaluator、fairness gate evaluator）并替换 pack 内三处 lambda 装配，修复 wrapper 参数签名以兼容位置参数调用方；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
103. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 90 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_domain_family_sort_key_for_runtime` 与 `_build_trust_challenge_id_for_runtime`，并替换剩余两处 lambda（domain-family 排序 key 与 trust challenge id 生成器）以实现 `app_factory` lambda-zero 基线；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
104. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 91 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_run_route_guard_with_http_bridge`，并将 replay/trust/trust-challenge/fairness/review/panel/registry/judge-command/assistant/case-read 十条 async guard 统一改为共享桥接器，保持各路由错误码与 detail 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
105. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 92 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_policy_registry_profiles_payload_for_runtime` 与 `_build_policy_registry_profile_payload_for_runtime`，将 `/internal/judge/policies` 与 `/internal/judge/policies/{policy_version}` 的 payload 组装改为复用共享 helper，并通过 `serialize_policy_profile_with_domain_family` 预绑定保持 domain-family 序列化语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
106. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 93 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_dispatch_receipt_payload_for_runtime`，统一 `/internal/judge/v3/phase/cases/{case_id}/receipt` 与 `/internal/judge/v3/final/cases/{case_id}/receipt` 的 dispatch receipt 读取与 404 语义，保持序列化合同不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
107. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 94 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_validate_contract_or_raise_http_500_for_runtime`，并将 case overview/courtroom read-model/drilldown bundle/evidence-claim ops queue 四条 read 路由的合同校验异常处理统一收敛为单点 helper，保持 contract code 与 500 错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
108. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 95 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_await_payload_or_raise_http_500_for_runtime`，并将 `ops/read-model/pack` 与 `panels/runtime/profiles` 两条合同型 awaitable 路由改为共享 helper，保持 ValueError→contract violation 的 500 映射语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
109. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 96 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 registry ready+payload 组合 helper（`_build_registry_profiles_payload_with_ready_for_runtime`、`_build_registry_profile_payload_with_ready_for_runtime`、`_build_policy_registry_profiles_payload_with_ready_for_runtime`、`_build_policy_registry_profile_payload_with_ready_for_runtime`），并将 policy/prompt/tool 的 list/get 六条路由改为统一装配，保持 readiness 时序与 404 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
110. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 97 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_await_payload_or_raise_http_422_for_runtime`、`_await_payload_or_raise_http_404_for_runtime`、`_await_payload_or_raise_http_422_404_for_runtime` 与 `_build_payload_or_raise_http_404_for_runtime`，统一 422/404 路由异常桥接入口并减少尾段重复 try/except 装配噪音；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
111. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 98 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `GET /internal/judge/review/cases/{case_id}`、`POST /internal/judge/review/cases/{case_id}/decision` 与 `GET /internal/judge/alerts/ops-view` 改为复用 await-error bridge helper，保持 `ValueError/LookupError` 到 422/404 的错误语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
112. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 99 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 将 `POST /internal/judge/alerts/outbox/{event_id}/delivery` 与 `GET /internal/judge/rag/diagnostics` 改为复用 `_build_payload_or_raise_http_404_for_runtime`，统一 lookup 错误桥接并保持原有 404 detail 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
113. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 100 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_raise_registry_version_not_found_lookup_error`，并将 registry activate/rollback 两条路由的 LookupError→404 桥接统一到共享 helper，保持 `registry_version_not_found` 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
114. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 101 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_registry_audits_payload_for_runtime`、`_build_registry_releases_payload_for_runtime` 与 `_build_registry_release_payload_for_runtime`，并将 registry audits/releases/list/get 三条路由改为复用共享 payload helper，保持 ValueError→registry error code 与 release not-found 语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
115. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 102 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_review_cases_list_payload_for_runtime`，将 `GET /internal/judge/review/cases` 的 filters 归一化与 payload 装配从路由体内联块收敛到共享 helper，并通过 `_await_payload_or_raise_http_422_for_runtime` 统一 422 错误桥接；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
116. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 103 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_ops_read_model_pack_payload_for_runtime`、`_build_review_case_detail_payload_for_runtime` 与 `_build_alert_ops_view_payload_for_runtime`，并将 `GET /internal/judge/ops/read-model/pack`、`GET /internal/judge/review/cases/{case_id}`、`GET /internal/judge/alerts/ops-view` 三条路由改为复用共享 payload helper，保持 contract 错误桥接与查询语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
117. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 104 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_case_alerts_payload_for_runtime` 与 `_build_alert_outbox_payload_for_runtime`，并将 `GET /internal/judge/cases/{case_id}/alerts` 与 `GET /internal/judge/alerts/outbox` 两条路由改为复用共享 payload helper，保持告警读取与出站列表语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
118. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 105 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_panel_runtime_profiles_payload_for_runtime` 与 `_build_panel_runtime_readiness_payload_for_runtime`，并将 `GET /internal/judge/panels/runtime/profiles` 与 `GET /internal/judge/panels/runtime/readiness` 两条路由改为复用共享 payload helper，保持 panel runtime 过滤/排序与 contract 桥接语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
119. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 106 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_replay_report_payload_for_runtime`、`_build_replay_reports_payload_for_runtime`、`_build_fairness_calibration_pack_payload_for_runtime` 与 `_build_fairness_policy_calibration_advisor_payload_for_runtime`，并将 `GET /internal/judge/cases/{case_id}/replay/report`、`GET /internal/judge/cases/replay/reports`、`GET /internal/judge/fairness/calibration-pack`、`GET /internal/judge/fairness/policy-calibration-advisor` 四条路由改为复用共享 payload helper，保持 replay/fairness 校准读取语义与 contract 桥接不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。
120. 2026-04-23：推进 `ai-judge-p33-app-factory-hotspot-split-v4` 第 107 子批，在 `/Users/panyihang/Documents/EchoIsle/ai_judge_service/app/app_factory.py` 新增 `_build_trust_challenge_ops_queue_payload_for_runtime`、`_build_trust_challenge_request_payload_for_runtime` 与 `_build_trust_challenge_decision_payload_for_runtime`，并将 `GET /internal/judge/trust/challenges/ops-queue`、`POST /internal/judge/cases/{case_id}/trust/challenges/request`、`POST /internal/judge/cases/{case_id}/trust/challenges/{challenge_id}/decision` 三条路由改为复用共享 payload helper，保持 trust challenge 队列筛选/状态流转语义不变；通过 `ruff + pytest`（`test_app_factory + test_registry_governance_routes + test_judge_trace_replay_routes + test_assistant_agent_routes + test_review_alert_routes + test_judge_command_routes`）回归确认。

---

## 12. 本轮启动检查清单

1. 开发前运行 `pre-module-prd-goal-guard`（按模块执行）。
2. 涉及 API/DTO/错误码变更时，同轮同步调用方、测试与文档。
3. 与真实环境相关结论必须标注 `on-env`，本地阶段不得宣称 `pass`。
4. 每完成一个模块都回写当前计划矩阵与同步历史。
5. 通过架构方案第 13 章一致性检查清单后，再生成下一轮计划。

---

## 13. 架构方案第13章一致性校验（计划生成前置）

1. 角色一致性：不合并或绕过 8 Agent 职责边界，保持 `workflow_roles` 可追踪。
2. 数据一致性：六对象仍为主链语义，路由重构不改变对象合同。
3. 门禁一致性：`Fairness Sentinel` 继续处于终判前位置，override 可审计。
4. 边界一致性：`NPC/Room QA` 仅 advisory-only，不写官方 verdict/fairness/trust。
5. 跨层一致性：契约变更同轮更新调用方、测试与文档，不保留长期 alias 双轨。
6. 收口一致性：真实环境项继续区分 `local_reference_ready` 与 `pass`。
