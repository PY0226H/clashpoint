# API058 开发文档：`GET /api/debate/ops/rbac/roles`

更新时间：2026-04-08
范围：围绕 `GET /api/debate/ops/rbac/roles` 的完整链路（路由、中间件、RBAC 服务、前端消费、运维治理）进行问题分析与方案设计。

---

## 一、当前实现快照（问题分析基线）

1. 路由：`/api/debate/ops/rbac/roles` 挂在 `phone_required_api` 下，先过 `verify_token_header_only` 与 `require_phone_bound`。
2. Handler：`list_ops_role_assignments_handler` 已接入 user/ip 双限流、读审计日志、专项计数与延迟采样，再调用 `state.list_ops_role_assignments_by_owner(&user)`。
3. 权限校验：`list_ops_role_assignments_by_owner` 内部调用 `ensure_platform_admin_for_ops_rbac`，仅平台 Owner 可访问。
4. Owner 真源：`get_platform_admin_user_id()` 当前读取 `platform_admin_owners`（不再硬编码 `user_id=1`）。
5. 查询逻辑：`platform_user_roles JOIN users`，按 `updated_at DESC, user_id DESC` 返回全量角色分配。
6. OpenAPI：`GET /api/debate/ops/rbac/roles` 已声明 `200/401/403/409/429/500`；`PUT/DELETE /api/debate/ops/rbac/roles/{userId}` 也已补齐中间件/输入异常状态码声明。
7. 前端消费：`frontend/packages/ops-domain` 与 `OpsConsolePage` 直接调用并展示角色分配结果。
8. 测试：已覆盖 `rbac/me`、`rbac/roles`、`rbac/roles/{userId}` 的 route 回归矩阵（含 `200/400/401/403/404/409/429/500` 关键路径）。

---

## 二、完整问题分析（深入版）

## 2.1 问题总览

| 风险ID | 问题 | 严重级别 | 当前状态 |
|---|---|---|---|
| `RBACROLES-R1` | OpenAPI 与运行态错误谱系不一致（仅 200/409） | P0 | 已解决 |
| `RBACROLES-R2` | 缺 route 级回归矩阵（中间件+handler） | P0 | 已解决 |
| `RBACROLES-R3` | Owner 真源缺失时的恢复路径不显式（500） | P0 | 已解决 |
| `RBACROLES-R4` | 权限拒绝用 `409` 语义存在跨端理解成本 | P1 | 已解决（阶段决策：RBAC 管理面保持 409） |
| `RBACROLES-R5` | 接口无专项限流，控制平面读接口抗滥用能力弱 | P1 | 部分解决（GET 已落地） |
| `RBACROLES-R6` | 缺控制平面专项指标与 tracing 标签 | P1 | 部分解决（日志计数已落地） |
| `RBACROLES-R7` | 角色列表读取无审计落库（谁在看、何时看） | P1 | 部分解决（日志审计已落地） |
| `RBACROLES-R8` | 错误模型依赖字符串，客户端分支稳定性弱 | P1 | 部分解决（machine code 归一 + 前端 code 优先） |
| `RBACROLES-R9` | 全量返回无分页/上限，长期存在响应膨胀风险 | P2 | 未解决 |
| `RBACROLES-R10` | `i64 -> u64` 强转未显式边界保护 | P2 | 已解决（显式安全转换已落地） |
| `RBACROLES-R11` | 返回邮箱+姓名，PII 最小化策略未文档化 | P2 | 未解决 |
| `RBACROLES-R12` | 手机门禁是否适用于 Owner 应急运维场景待定 | P2 | 待产品策略 |

---

## 2.2 逐项深度分析

### `RBACROLES-R1` OpenAPI 与运行态不一致（P0）

- 现象：`list_ops_role_assignments_handler` 注解只写 `200/409`。
- 事实：运行态还会出现 `401`（token）、`403`（phone gate）、`500`（owner 真源缺失或 SQL 异常）。
- 风险：
  1. SDK/前端错误分支覆盖不完整。
  2. 联调时“文档显示不会出现”的状态码被线上返回，导致返工。

### `RBACROLES-R2` 缺 route 级回归（P0）

- 现象：当前主要有 model 测试，缺此接口 route 测试。
- 风险：
  1. 中间件顺序或行为变化（鉴权/手机门禁）可能静默破坏接口。
  2. OpenAPI 改动与真实行为易再次漂移。

### `RBACROLES-R3` owner 真源缺失恢复路径不显式（P0）

- 现象：`platform_admin_owners` 缺失会触发 `platform_owner_not_configured`（500）。
- 风险：
  1. 一旦配置异常，整个角色管理读写面不可用。
  2. 缺少标准化 runbook 时，值班定位恢复慢。

### `RBACROLES-R4` 权限拒绝状态码语义争议（P1）

- 现象：非 Owner 返回 `409 DebateConflict`。
- 风险：
  1. 跨端通常将权限拒绝理解为 `403`，而 `409` 常用于资源冲突。
  2. 语义不统一会增加客户端特判与认知负担。
- 收口结论：RBAC 管理面本阶段冻结为 `409`，并在 `GET/PUT/DELETE /ops/rbac/roles*` 的 OpenAPI 与 route 回归中保持一致。

### `RBACROLES-R5` 缺限流（P1）

- 现象：该 GET 接口暂无 user/ip 限流闸门。
- 风险：
  1. 被脚本高频轮询时会增加 DB 读压力。
  2. 控制平面接口缺抗滥用能力，不利于值班稳定性。

### `RBACROLES-R6` 可观测不足（P1）

- 现象：缺 `ops_rbac_roles_list` 专项指标、延迟分布、拒绝原因分桶。
- 风险：
  1. 权限问题与数据库问题不易快速区分。
  2. 无法建立告警阈值和趋势基线。

### `RBACROLES-R7` 读审计缺失（P1）

- 现象：角色变更有审计，但角色列表读取暂无显式审计。
- 风险：
  1. 角色名单属于敏感控制面数据，缺“谁看过”追踪。
  2. 安全事件追溯能力不足。

### `RBACROLES-R8` 错误模型机读稳定性不足（P1）

- 现状：客户端 `toOpsDomainError` 已升级为“前缀剥离 + code 归一（如 `ops_permission_denied:role_manage`）+ code 优先分支”。
- 风险：
  1. 平台级响应仍是 `ErrorOutput.error` 单字段，尚未统一到 `{code,message,details?}`。
  2. 多端要彻底摆脱文案耦合，仍需后端统一结构化输出。

### `RBACROLES-R9` 全量返回长期风险（P2）

- 现象：当前无分页/limit。
- 当前合理性：角色分配规模目前极小。
- 长期风险：当组织规模扩展或引入多租户后，响应体与查询成本会变大。

### `RBACROLES-R10` ID 强转边界防护不足（P2）

- 现状：`map_assignment_row` 已改为显式 `u64::try_from` 安全转换，异常时返回稳定 server code。
- 风险：
  1. 正常路径风险已显著降低。
  2. 后续仍需结合数据质量巡检，确保异常码不会被静默吞掉。

### `RBACROLES-R11` PII 最小化策略未固化（P2）

- 现象：接口返回 `userEmail + userFullname`。
- 风险：
  1. 目前是 owner-only，可接受；但缺最小化策略文档化（哪些场景必须回传完整 PII）。
  2. 后续若权限边界变化，容易形成数据暴露面扩大。

### `RBACROLES-R12` 手机门禁策略待产品冻结（P2）

- 现象：接口强依赖 `require_phone_bound`。
- 风险：
  1. Owner 应急账号若未绑手机，无法查看角色分配，可能影响应急操作。
  2. 但从安全角度看，门禁可提升控制面安全。
- 结论：需要产品与安全策略共同定稿。

---

## 三、解决方案（企业主流 + 推荐）

## 3.1 方案候选

### 方案 A（推荐，短中期主流）：契约与可观测先收口

核心目标：先把“文档-运行态一致性”和“回归防线”补齐，快速降低生产不确定性。

- 关键动作：
  1. OpenAPI 补齐 `401/403/500`（保留 `200/409`）。
  2. 增加 route 回归：`200/401/403/409/500`。
  3. 增加最小指标：`success/forbidden/conflict/error` 计数 + `latency`。
  4. 增加 owner 真源缺失 runbook（自动修复或手动恢复步骤）。

- 优点：
  1. 投入低、见效快。
  2. 直接解决 P0 风险。
  3. 不引入大规模架构变更。

### 方案 B（推荐，企业标准化）：RBAC 管理面治理统一

核心目标：把 `rbac/me`、`rbac/roles`、`rbac/roles/{userId}` 三个接口作为一个治理域统一收敛。

- 关键动作：
  1. 统一结构化错误模型：`{ code, message, details? }`。
  2. 统一权限拒绝语义（是否从 `409` 迁移到 `403`，需一次性冻结规范）。
  3. 引入读取审计（至少记录 owner、请求时间、结果数、request_id）。
  4. 增加 `revision` 字段为前端缓存/刷新提供稳定依据。

- 优点：
  1. 跨端行为更稳定。
  2. 值班排障效率显著提升。
  3. 为后续角色治理扩展打基础。

### 方案 C（长期）：策略引擎与数据最小化升级

- 关键动作：
  1. 抽象 policy 层统一权限判定。
  2. 按场景分级输出 PII（可选脱敏模式）。
  3. 分页化与多租户扩展预案。

- 结论：当前阶段可规划但不建议立即重投入。

---

## 3.2 推荐落地路径（按优先级）

### P0（必须）

1. 补 `GET /api/debate/ops/rbac/roles` OpenAPI 响应码：`401/403/500`。
2. 补 route 用例：`200/401/403/409`，并覆盖 owner 真源缺失 `500`。
3. 输出 owner 真源异常 runbook（值班可执行）。

### P1（强烈建议）

1. 接入接口限流（user + ip，轻量阈值即可）。
2. 增加控制平面专项指标与 tracing 标签。
3. 为读取动作增加审计记录（至少结构化日志）。
4. 推进 RBAC 管理面结构化错误模型统一。

### P2（迭代增强）

1. 明确是否分页化，并给出触发阈值（例如角色数 > 100）。
2. 增加 `revision` 字段与前端缓存策略联动。
3. 完成手机门禁与应急运维策略裁决。

---

## 3.3 验收标准（Definition of Done）

1. OpenAPI 与运行态一致：`200/401/403/409/500` 可在契约中体现。
2. route 回归测试覆盖主错误路径并稳定通过。
3. owner 真源缺失场景有明确恢复手册并可演练。
4. 具备基础可观测（成功/失败/拒绝/延迟）与最小审计证据。
5. 客户端对错误分支不再依赖文案字符串。

---

## 四、未解决问题（本轮保留）

1. `RBACROLES-R8`：`ErrorOutput` 仍是单字段，`{code,message,details?}` 平台级结构化输出尚未统一。
2. `RBACROLES-R11`：PII 最小化策略是否在 owner-only 场景继续保持全量返回。
3. `RBACROLES-R12`：手机门禁是否对 Owner 应急场景保留豁免机制。
4. 多租户/多 owner 模式暂不在本轮范围。

---

## 五、结论

`GET /api/debate/ops/rbac/roles` 当前功能链路可用，但作为控制平面接口，其核心风险在于“契约漂移 + 缺 route 防线 + 缺可观测/审计”。建议按 **方案 A（立即收口）+ 方案 B（治理统一）** 两段式推进，先稳住正确性，再提升长期可维护性。

---

## 六、本轮执行结果（2026-04-08）

已落地（对应计划执行）：

1. `RBACROLES-R1`：`GET /api/debate/ops/rbac/roles` OpenAPI 已补齐 `401/403/500`（并保留 `200/409`）。
2. `RBACROLES-R2`：route 回归已补齐 `200/401/403/409/500`，并通过针对性测试。
3. `RBACROLES-R3`：owner 真源缺失场景已具备可复现回归与恢复 runbook。
4. `RBACROLES-R5`：接口已接入 user/ip 双限流，新增 `429 rate_limit_exceeded:ops_rbac_roles_list`。
5. `RBACROLES-R6`：已增加 `ops_rbac_roles_list_*` 请求/成功/失败/拒绝/限流计数与延迟采样日志字段。
6. `RBACROLES-R7`：已增加角色列表读取审计化结构日志（`audit_event`、`user_id`、`request_id`、`result_count`）。
7. `RBACROLES-R4`：权限拒绝语义已在 RBAC 管理面冻结为 `409`，并通过 `GET/PUT/DELETE /ops/rbac/roles*` 路由回归对齐。
8. `RBACROLES-R8`：后端错误码已归一化（`ops_permission_denied:role_manage`、`ops_role_invalid`、`ops_role_target_user_not_found`），前端 `toOpsDomainError` 已改为 code 归一与优先分支。
9. `RBACROLES-R10`：`map_assignment_row` 已由 `as` 强转改为显式 `i64 -> u64` 安全转换，异常时返回稳定 server code。
10. `RBACROLES-R1/R2` 扩展：`PUT/DELETE /api/debate/ops/rbac/roles/{userId}` OpenAPI 响应谱系与 route 回归矩阵已补齐。

部分完成 / 待后续：

1. `RBACROLES-R8`：结构化 payload 仍需跨 `rbac/me`、`rbac/roles`、`rbac/roles/{userId}` 统一为 `{code,message,details?}`。
2. `RBACROLES-R9/R11/R12`：分页预案、PII 最小化、手机门禁策略仍待产品与工程联合收口。
