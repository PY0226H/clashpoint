# AI_Judge 下一迭代开发计划（2026-04-14）

状态：执行中（已进入模块排期）  
关联主计划：[当前开发计划](/Users/panyihang/Documents/EchoIsle/docs/dev_plan/当前开发计划.md)

## 1. 迭代目标

1. 保持现有主链稳定（phase/final/trace/replay/failed/review queue 不回退）。
2. 将 P5 从 `local_reference_pass` 推进到“真实环境可冻结”的收口态。
3. 启动 P6 三个核心能力的工程化首版：`fairness benchmark freeze`、`Claim Graph`、`Policy Registry`。

## 2. 基线快照

1. 已完成：final 展示字段硬切、trace/replay、failed callback、输入盲化拒绝。
2. 已完成：公平门禁 phase1/phase2（swap/style/panel + review queue + manual decision）。
3. 进行中：`ai-judge-p5-real-calibration-on-env`（当前 `local_reference_pass`，真实环境证据未冻结）。

## 3. 下一迭代模块

| 模块 | 优先级 | 状态 | 目标 | DoD | 验证 |
| --- | --- | --- | --- | --- | --- |
| ai-judge-fairness-benchmark-freeze | P0 | 已完成 | 冻结 fairness 阈值与口径 | 产出 benchmark 阈值表、告警阈值、基线证据路径；接入文档与脚本 | `bash scripts/harness/tests/test_ai_judge_fairness_benchmark_freeze.sh` |
| ai-judge-p5-real-calibration-on-env | P0 | 进行中 | 完成 real 环境证据冻结 | `REAL_CALIBRATION_ENV_READY=true` 且五轨道具备 `REAL_ENV_EVIDENCE/DATASET_REF`，状态为 `pass` | `bash scripts/harness/ai_judge_p5_real_calibration_on_env.sh` |
| ai-judge-claim-graph-bootstrap | P1 | 已完成 | 建立 Claim Graph 最小主链 | final payload 输出 `claimGraph/claimGraphSummary`；冲突与未回应主张可追踪 | `python -m pytest -q ai_judge_service/tests/test_judge_mainline.py ai_judge_service/tests/test_app_factory.py` |
| ai-judge-policy-registry-bootstrap | P1 | 待开始 | Prompt/Tool/Policy 注册中心最小闭环 | registry 可配置、可版本化、可在 runtime 读取 | 配置测试 + app_factory 回归 |
| ai-judge-trust-attestation-mvp | P2 | 待开始 | 承诺与证明最小可追踪链路 | trace/report 具备 commitment 字段与校验入口 | trace/replay 回归 + 文档证据 |

## 4. 执行顺序

1. 并行等待真实环境窗口，随时执行 `ai-judge-p5-real-calibration-on-env` 收口。
2. 进入 `ai-judge-policy-registry-bootstrap`。
3. 最后推进 `ai-judge-trust-attestation-mvp` 初版。

## 5. 风险与应对

1. 真实环境持续不可用：保持 `local_reference_pass`，禁止伪造 `pass`。
2. Claim Graph 设计膨胀：先交付最小主链，不做一次性大而全。
3. Registry 改造影响面扩大：坚持 hard-cut 主链，不做长期双轨兼容。

## 6. 退出条件（本迭代完成判据）

1. `ai-judge-fairness-benchmark-freeze` 完成并固化阈值文档。
2. `ai-judge-p5-real-calibration-on-env` 至少具备“随环境就绪即一键收口”的执行包。
3. Policy Registry 至少完成一个可回归、可追踪的最小版本，并衔接 claim graph 主链。
